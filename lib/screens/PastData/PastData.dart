import 'package:flutter/material.dart';

class PastData_Page extends StatelessWidget {
  const PastData_Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Past Data')),
    );
  }
}
